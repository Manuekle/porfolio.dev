---
interface Props {
  class?: string
  [key: string]: any
}

const { class: className, ...rest } = Astro.props
---

<div class={`rounded-2xl bg-white p-6 shadow-sm ring-1 ring-black/[0.04] ${className || ''}`} {...rest}>
  <slot />
</div>
