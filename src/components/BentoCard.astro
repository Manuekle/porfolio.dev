---
interface Props {
  class?: string
  [key: string]: any
}

const { class: className, ...rest } = Astro.props
---

<div class={`rounded-2xl border-3 border-white bg-[#F8F8F8] p-6 shadow-md ${className || ''}`} {...rest}>
  <slot />
</div>
