# manudev — portfolio

Personal portfolio of Manuel Erazo. Minimal academic-journal layout: a fixed
identity column beside a single reading column. Bilingual (ES / EN), light and
dark paper.

Built with [Astro](https://astro.build) + React islands + TypeScript + Tailwind v4.

## Design rules

These are deliberate and enforced in `src/styles/global.css`:

- **No bold.** Every weight is `400`; hierarchy comes from size, colour and rules.
- **No uppercase.** Sentence case everywhere.
- **Nothing rounded.** `border-radius: 0` is set on `*`.
- **Titles and subtitles track at `-0.05em`** (`--track-title`).
- **Type:** Cooper (serif) for titles and subtitles, Saans for UI titles and
  navigation, Inter for body copy. All self-hosted from `public/assets/font/`.

## Stack

- **Astro 6** — static output.
- **React 19** — the page shell (language + theme toggles, scroll spy, reveal).
- **Tailwind v4** + CSS custom properties for the paper / ink palette.
- Brand logos from [Simple Icons](https://simpleicons.org) (CC0-1.0), baked into
  `src/data/brands.ts` so nothing is fetched at runtime.

## Commands

| Command            | Action                        |
| :----------------- | :---------------------------- |
| `pnpm install`     | Install dependencies          |
| `pnpm dev`         | Dev server at `localhost:4321`|
| `pnpm build`       | Build to `./dist/`            |
| `pnpm preview`     | Preview prod build locally    |
| `pnpm astro check` | TypeScript + Astro diagnostics|

## Structure

```
src/
├── components/
│   ├── App.tsx           root state (lang, theme, scroll spy, reveal)
│   ├── Sidebar.tsx       identity column
│   ├── Mark.tsx          BrandMark, Monogram, LogoTile, SiteLogo
│   ├── Section.tsx       shared section header
│   └── sections/         Hero, About, Experience, Projects, Stack,
│                         Hackathons, Education, Contact
├── data/
│   ├── portfolio.ts      bilingual copy, experience, projects, stack, education
│   └── brands.ts         generated brand logo paths
├── layouts/Layout.astro
├── pages/index.astro
└── styles/global.css
public/
├── new-icon.png          the portfolio mark (source for every icon)
└── assets/
    ├── mark.png          transparent mark, used as a CSS mask
    ├── avatar.png        square portrait, cropped from pic.PNG
    ├── font/             cooper, saans, inter
    └── CV_Manuel_Erazo_{ES,EN}.pdf
```

## Logos

Tool and company logos are real brand marks, drawn from `src/data/brands.ts` and
tinted with `currentColor`. Regenerate the set after editing the slug list:

```bash
node scripts/gen-logos.mjs
```

Anything Simple Icons does not carry — employers, schools, certificate issuers
and the projects themselves — is generated by `scripts/gen-marks.cjs`, which
fetches each organisation's own published icon and reduces it to a single ink
silhouette on a transparent field:

```bash
node scripts/gen-marks.cjs
```

The reduction matters. Published app icons arrive as colour artwork on rounded
plates and circles, which is exactly what this design does not use — dropped
straight in they read as stickers pasted onto the page, and they fight the
paper. Keying the glyph out of its plate leaves a mark the page can tint, sized
and weighted like every other mark in the column. Each entry points at its file
through `logoFile` (companies) or `logo` (projects); anything without one falls
back to a serif monogram.

| File | Source | How it is keyed |
| :--- | :----- | :-------------- |
| `orgs/gymrat.png` | gymratplus.com — `safari-pinned-tab.svg` | already a silhouette |
| `orgs/gymshark.png` | gymshark.com — `safari-pinned-tab.svg` | already a silhouette |
| `orgs/smurfitkappa.png` | smurfitkappa.com — `SW_LOGO_2COL.svg` | symbol only, wordmark dropped |
| `orgs/sena.png` | sena.edu.co — `sena.png` | mid tone, figure knocked out |
| `orgs/fup.png` | fup.edu.co — site icon | already a silhouette |
| `projects/creagent.png` | creagent.fun — `apple-icon.png` | mid tone, halftone smoothed |
| `projects/steve.png` | steve-manudev.vercel.app — `icon.svg` | light glyph off a dark plate |
| `projects/forge.png` | forgems.vercel.app — `logo.png` | low saturation, the star is a gradient |
| `projects/sira.png` | sira-fup.online — `apple-touch-icon.png` | dark glyph off a light plate |
| `projects/heymed.png` | heymed.fun — `favicon-180x180.png` | light glyph, trace thickened |
| `projects/hinomaru.png` | hinomaru.vercel.app — `apple-touch-icon.png` | dark glyph off a light plate |
| `projects/butterflyar.png` | Manuekle/butterflyar-mobile — `assets/icon/favicon.png` | already a silhouette |

## Assets

Every app icon derives from `public/new-icon.png`:

```bash
node scripts/gen-icons.cjs   # favicons + apple-touch + android-chrome
node scripts/gen-site-mark.cjs  # public/assets/mark.png (transparent mask)
node scripts/gen-og.cjs      # public/og-banner.png
node scripts/gen-avatar.cjs  # public/assets/avatar.png
```

## Theme + lang

Persisted in `localStorage` under `portfolio.prefs`. `Layout.astro` reads it
before first paint to avoid a flash.

## Deploy

Static output. Drop `dist/` on Vercel / Netlify / Cloudflare Pages.
